---
"@jmfederico/pi-web": patch
---

Add quick-switch model presets next to the prompt editor's model button. Save the current model and thinking level as a numbered preset chip with the `+` button, click a chip to apply both at once, and right-click a chip to remove it. Presets persist in the browser (up to 9) and apply to any session.
