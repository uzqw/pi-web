---
"@jmfederico/pi-web": patch
---

Let web UI sessions be pinned to the top of the session list. Each session row now stacks a pin button above the actions menu: pinning lifts a session above the rest (newest pin first), pinned rows can be dragged to reorder, and unpinning returns the session to its normal place in the tree.
