---
"@jmfederico/pi-web": patch
---

Let web UI list rows be pinned to the top of their section. Project, workspace, and session rows now stack a pin button above the actions menu: pinning lifts an item above the rest (newest pin first), pinned rows can be dragged to reorder, and unpinning returns the item to its normal place.
